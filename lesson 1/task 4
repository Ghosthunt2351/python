num = input('Введите целое положительное число: ')
big_num = 0
i = 0
while i < len(num):
  one_num = int(num) % 10 ** (i+1) // 10 ** i
  if big_num < one_num:
    big_num = one_num
  i += 1
print('Наибольшая цифра в вашем числе:', big_num)
