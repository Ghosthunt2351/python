# Создать текстовый файл (не программно), сохранить в нём несколько строк, выполнить подсчёт строк и слов в каждой строке.

with open("text_4.txt", 'r') as f_t2:
    content = f_t2.readlines()
    i = 0
    for line in content:
        i += 1
        print(f'В {i} строке {len(line.split())} слов(а)')
