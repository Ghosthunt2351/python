def fact(n):
  x = 1
  if n > 0:
    for i in range(1, n+1):
      x *= i
      yield x
  else:
    yield x


try:
  n = int(input('Введите целое число n: '))
  for el in fact(n):
    print(el)
except ValueError as err:
  print(err)
